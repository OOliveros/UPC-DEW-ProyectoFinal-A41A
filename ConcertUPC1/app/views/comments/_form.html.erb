<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user %>
    <%= f.input :message %>
    <%= f.label :group_id, :value => @concert.name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
